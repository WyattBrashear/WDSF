[build-system]
requires = ["setuptools>=45.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wdsf"
version = "1.0.0"
description = "Wyatt's Data Storage Format"
requires-python = ">=3.13"
dependencies = []
